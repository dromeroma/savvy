import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-water-layout',
  imports: [RouterOutlet],
  template: `<router-outlet />`,
})
export class WaterLayoutComponent {}

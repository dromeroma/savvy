import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';
@Component({ selector: 'app-pay-layout', imports: [RouterOutlet], template: `<router-outlet />` })
export class PayLayoutComponent {}
